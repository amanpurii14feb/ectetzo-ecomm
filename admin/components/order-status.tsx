"use client";import { useState } from "react";import { useRouter } from "next/navigation";export function OrderStatus({id,status}:{id:string;status:string}){const router=useRouter(),[busy,setBusy]=useState(false);async function change(v:string){setBusy(true);await fetch(`/api/admin/orders/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:v})});setBusy(false);router.refresh()}return <select className="adm-select" disabled={busy} value={status} onChange={e=>change(e.target.value)}>{["PENDING","CONFIRMED","PROCESSING","SHIPPED","DELIVERED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select>}
