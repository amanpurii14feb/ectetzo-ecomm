"use client";import { useMemo,useState } from "react";import Link from "next/link";import { Download,Search } from "lucide-react";import { PageHeader,StatusBadge } from "./ui";
type O={id:string;orderNumber:string;createdAt:string;customer:string;email:string;status:string;paymentStatus:string;items:number;total:number};const tone=(s:string)=>s==="DELIVERED"||s==="PAID"?"success":s==="CANCELLED"||s==="FAILED"?"danger":s==="PENDING"||s==="UNPAID"?"warning":"info";
export function OrdersTable({orders}:{orders:O[]}){const[q,setQ]=useState(""),[tab,setTab]=useState("ALL");const rows=useMemo(()=>orders.filter(o=>(tab==="ALL"||o.status===tab)&&`${o.orderNumber} ${o.customer} ${o.email}`.toLowerCase().includes(q.toLowerCase())),[orders,q,tab]);return <><PageHeader title="Orders" description="Review, fulfill and track customer orders." actions={<button className="adm-btn"><Download/>Export</button>}/><section className="adm-panel adm-list-panel"><div className="adm-tabs">{["ALL","PENDING","PROCESSING","DELIVERED","CANCELLED"].map(x=><button className={tab===x?"active":""} key={x} onClick={()=>setTab(x)}>{x[0]+x.slice(1).toLowerCase()}</button>)}</div><div className="adm-filterbar"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search orders or customers"/></label><select><option>Any payment status</option><option>Paid</option><option>Unpaid</option></select><input type="date"/></div><div className="adm-table-scroll"><table className="adm-data"><thead><tr><th>Order</th><th>Date</th><th>Customer</th><th>Payment</th><th>Fulfillment</th><th>Items</th><th>Total</th><th/></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><Link href={`/admin/orders/${o.id}`}><b>{o.orderNumber}</b></Link></td><td>{new Date(o.createdAt).toLocaleDateString("en-IN")}</td><td><b>{o.customer}</b><small>{o.email}</small></td><td><StatusBadge tone={tone(o.paymentStatus) as "success"}>{o.paymentStatus}</StatusBadge></td><td><StatusBadge tone={tone(o.status) as "success"}>{o.status}</StatusBadge></td><td>{o.items}</td><td><b>₹{o.total.toLocaleString("en-IN")}</b></td><td><Link className="adm-table-link" href={`/admin/orders/${o.id}`}>View</Link></td></tr>)}</tbody></table></div></section></>}
