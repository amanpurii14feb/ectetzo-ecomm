"use client";import { useState } from "react";
export function ContactForm(){const[form,setForm]=useState({name:"",contact:"",message:""}),[status,setStatus]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus("");const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const b=await r.json().catch(()=>({}));setStatus(r.ok?"Message received. Our team will contact you.":b.error??"Could not send message.");if(r.ok)setForm({name:"",contact:"",message:""});setBusy(false)}return <form onSubmit={submit} className="card grid gap-4 p-6"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="field" placeholder="Your name"/><input required value={form.contact} onChange={e=>setForm({...form,contact:e.target.value})} className="field" placeholder="Email or mobile"/><textarea required value={form.message} onChange={e=>setForm({...form,message:e.target.value})} className="field min-h-28" placeholder="Tell us how we can help"/><button disabled={busy} className="btn btn-dark w-fit">{busy?"Sending...":"Send message"}</button>{status&&<p role="status" className="text-sm">{status}</p>}</form>}
