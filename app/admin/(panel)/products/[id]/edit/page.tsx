import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import { ProductEditor } from "@/admin/components/product-editor";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await prisma.product.findUnique({where:{id}});if(!p)notFound();return <ProductEditor product={{id:p.id,name:p.name,slug:p.slug,brand:p.brand,category:p.category,description:p.description,price:p.price,mrp:p.mrp,stock:p.stock,rating:p.rating,reviews:p.reviews,badge:p.badge,color:p.color,images:p.images,active:p.active,specs:p.specs as Record<string,string>}}/>}
