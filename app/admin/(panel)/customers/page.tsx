import { prisma } from "@/lib/prisma";import { PageHeader,StatusBadge } from "@/admin/components/ui";import { Download,Search,UserPlus } from "lucide-react";export const dynamic="force-dynamic";
export default async function Page(){const users=await prisma.user.findMany({where:{role:"CUSTOMER"},include:{orders:{orderBy:{createdAt:"desc"}},addresses:true},orderBy:{createdAt:"desc"}});return <><PageHeader title="Customers" description="View customer profiles and purchase history." actions={<><button className="adm-btn"><Download/>Export</button><button className="adm-btn primary"><UserPlus/>Add customer</button></>}/><section className="adm-panel adm-list-panel"><div className="adm-tabs"><button className="active">All customers <span>{users.length}</span></button><button>New</button><button>Returning</button><button>High value</button><button>Inactive</button></div><div className="adm-filterbar"><label><Search/><input placeholder="Search name, email or phone"/></label></div><div className="adm-table-scroll"><table className="adm-data"><thead><tr><th>Customer</th><th>Phone</th><th>Orders</th><th>Total spent</th><th>Last order</th><th>Status</th></tr></thead><tbody>{users.map(u=>{const spent=u.orders.reduce((s,o)=>s+o.total,0);return <tr key={u.id}><td><div className="adm-customer"><span>{(u.name??u.email)[0]?.toUpperCase()}</span><p><b>{u.name??"Customer"}</b><small>{u.email}</small></p></div></td><td>{u.phone??u.addresses[0]?.phone??"—"}</td><td>{u.orders.length}</td><td><b>₹{spent.toLocaleString("en-IN")}</b></td><td>{u.orders[0]?.createdAt.toLocaleDateString("en-IN")??"—"}</td><td><StatusBadge tone={u.orders.length?"success":"neutral"}>{u.orders.length?"Active":"New"}</StatusBadge></td></tr>})}</tbody></table></div></section></>}
